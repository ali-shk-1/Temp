# Prisma / app database connection — REQUIRED.
# Build this from your DB_HOST / DB_PORT / DB_NAME / DB_USER / DB_PASSWORD values:
# postgresql://<user>:<password>@<host>:<port>/<database>
DATABASE_URL="postgresql://postgres:123%23Ali123@localhost:5432/school_db"

# Keep these too if other app code reads them individually
DB_HOST=localhost
DB_PORT=5432
DB_NAME=school_db
DB_USER=postgres
DB_PASSWORD="123#Ali123"

# JWT
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=8h

# Server
PORT=5000
NODE_ENV=development

# Email / SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=alis629926@gmail.com
SMTP_PASS=rdmj wixm eafd hfpt
SMTP_FROM="Muhammad Ali <alis629926@gmail.com>"
SMTP_SECURE=false
